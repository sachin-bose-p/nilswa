version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: nilswa
      POSTGRES_PASSWORD: nilswapassword
      POSTGRES_DB: nilswadb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
